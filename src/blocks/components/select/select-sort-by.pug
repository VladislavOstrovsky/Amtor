div.select-container.sort
  p.select-label Сортировать:
  select(name='sort_by')
    option(value='') Другой вариант
    option(value='desc') По убыванию цены
    option(value='asc') По возрастанию цены
    option(value='rating') По рейтингу
    option(value='alphabetically') По алфавиту
